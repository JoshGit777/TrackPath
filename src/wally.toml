[package]
name = "joshgit777/trackpath"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["wally.toml", "TrackPath.luau"]


[dependencies]
